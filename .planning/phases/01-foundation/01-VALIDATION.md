---
phase: 1
slug: foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.0 |
| **Config file** | `vitest.config.mts` — does not yet exist, create in Wave 0 |
| **Quick run command** | `npx tsc --noEmit` |
| **Full suite command** | `npx tsc --noEmit && npm run build` |
| **Estimated runtime** | ~30 seconds (type-check + build) |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit`
- **After every plan wave:** Run `npx tsc --noEmit && npm run build`
- **Before `/gsd:verify-work`:** Full suite must be green + `docker compose up --wait` succeeds
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01-01-01 | 01 | 1 | SCAF-01 | type-check | `npx tsc --noEmit` | ❌ Wave 0 | ⬜ pending |
| 01-01-02 | 01 | 1 | SCAF-01 | build-check | `npm run build` | ❌ Wave 0 | ⬜ pending |
| 01-02-01 | 02 | 1 | SCAF-06 | type-check | `npx tsc --noEmit` | ❌ Wave 0 | ⬜ pending |
| 01-03-01 | 03 | 2 | SCAF-03 | smoke | `docker compose up --wait` | ❌ Wave 0 | ⬜ pending |
| 01-04-01 | 04 | 2 | SCAF-06 | manual | `npm run db:migrate` | ❌ Wave 0 | ⬜ pending |
| 01-05-01 | 05 | 1 | SCAF-02 | manual | inspect CLAUDE.md | ❌ Wave 0 | ⬜ pending |
| 01-06-01 | 06 | 1 | SCAF-05 | manual | inspect .mcp.json | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `vitest.config.mts` — Vitest configuration (needed for Phase 2+)
- [ ] `src/tests/setup.ts` — shared test setup file
- [ ] `tsconfig.json` — generated by Next.js scaffold; verify `strict: true` is set
- [ ] Vitest devDependencies: `vitest @vitejs/plugin-react @testing-library/react @testing-library/jest-dom jsdom`

*Existing infrastructure covers all phase requirements once scaffold is created.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Docker stack health checks passing | SCAF-03 | Requires Docker daemon to be running | Run `docker compose up --wait`; all 4 services (app, db, redis, nginx) must show healthy |
| Drizzle migrations run cleanly | SCAF-06 | Requires live PostgreSQL connection | Run `npm run db:migrate` against a fresh database; must complete with 0 errors |
| CLAUDE.md contains all required rules | SCAF-02 | Content review | Open CLAUDE.md, verify presence of: integer pence rule, append-only ledger rule, verifySession() convention, server-only boundary rule |
| Context7 MCP responds to queries | SCAF-05 | Requires MCP runtime | Open Claude Code in this project; run a Context7 query to confirm it resolves library docs |
| GitHub MCP auth resolved | SCAF-05 | Requires GitHub token | Confirm no auth error when Claude Code starts; document fix in CLAUDE.md |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending

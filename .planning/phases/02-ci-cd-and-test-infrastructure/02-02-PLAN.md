---
phase: 02-ci-cd-and-test-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/smoke.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - SCAF-04
  - SCAF-07
must_haves:
  truths:
    - "Playwright is installed with Chromium and Firefox browsers"
    - "A smoke test navigates to / and asserts redirect to /login"
    - "CI workflow defines two jobs: test (lint+type-check+vitest+build) and e2e (Playwright)"
    - "CI triggers on PRs to main and direct pushes to main"
    - "CI uses hashFiles-based npm caching via actions/setup-node cache option"
    - "CI wires Postgres 16 and Redis 7 as service containers with health checks"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright test configuration with webServer and two browser projects"
      contains: "webServer"
    - path: "e2e/smoke.spec.ts"
      provides: "Smoke test proving app boots and proxy redirect works"
      contains: "toHaveURL"
    - path: ".github/workflows/ci.yml"
      provides: "Complete CI pipeline with test and e2e jobs"
      contains: "needs: test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "playwright.config.ts"
      via: "e2e job runs npx playwright test which reads playwright.config.ts"
      pattern: "npx playwright test"
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "test job runs npm run lint, npm run type-check, npm test, npm run build"
      pattern: "npm run lint"
    - from: "playwright.config.ts"
      to: "e2e/smoke.spec.ts"
      via: "testDir: './e2e' points Playwright to e2e directory"
      pattern: "testDir.*e2e"
    - from: ".github/workflows/ci.yml"
      to: ".next/"
      via: "test job uploads .next/ artifact, e2e job downloads it"
      pattern: "upload-artifact"
---

<objective>
Install Playwright, create the Playwright config and smoke test, and create the complete GitHub Actions CI workflow with both the `test` job and `e2e` job.

Purpose: Establishes the CI pipeline that gates all future PRs and the E2E infrastructure that proves the app boots correctly. The `test` job runs lint, type-check, vitest, and build. The `e2e` job runs Playwright against the production build.
Output: `playwright.config.ts`, `e2e/smoke.spec.ts`, `.github/workflows/ci.yml`
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ci-cd-and-test-infrastructure/02-CONTEXT.md
@.planning/phases/02-ci-cd-and-test-infrastructure/02-RESEARCH.md

<interfaces>
<!-- Login page structure (smoke test must match this) -->
From src/app/(auth)/login/page.tsx:
```typescript
export default function LoginPage() {
  return (
    <div>
      <h1>Login</h1>
      <p>Authentication will be implemented in Phase 3.</p>
    </div>
  )
}
```
Note: Login page has NO form inputs yet. Smoke test should assert the h1 "Login" text, NOT input[type="email"].

<!-- next.config.ts (output: standalone affects build artifact) -->
From next.config.ts:
```typescript
const nextConfig: NextConfig = {
  output: 'standalone',
}
```

<!-- .env.example (env vars needed for CI service containers) -->
From .env.example:
```
DATABASE_URL=postgresql://potplanner:potplanner@localhost:5432/potplanner
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-production
NODE_ENV=development
```

<!-- package.json scripts that CI will call -->
After Plan 01 completes (or if running in parallel, these will exist):
```json
{
  "lint": "eslint .",
  "test": "vitest run",
  "type-check": "tsc --noEmit",
  "build": "next build",
  "start": "next start"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and create config + smoke test</name>
  <files>playwright.config.ts, e2e/smoke.spec.ts</files>
  <read_first>
    - package.json (confirm current devDependencies)
    - src/app/(auth)/login/page.tsx (login page structure for smoke test assertions)
    - next.config.ts (output: standalone affects webServer behavior)
    - .planning/phases/02-ci-cd-and-test-infrastructure/02-RESEARCH.md (Playwright config pattern, smoke test pattern)
  </read_first>
  <action>
1. Install Playwright as a devDependency:
   ```bash
   npm install -D @playwright/test
   ```

2. Install Chromium and Firefox browsers with system dependencies:
   ```bash
   npx playwright install chromium firefox --with-deps
   ```

3. Create `playwright.config.ts` at repo root with this exact content:
   ```typescript
   import { defineConfig, devices } from '@playwright/test'

   export default defineConfig({
     testDir: './e2e',
     fullyParallel: true,
     forbidOnly: !!process.env.CI,
     retries: process.env.CI ? 2 : 0,
     workers: process.env.CI ? 1 : undefined,
     reporter: process.env.CI ? 'github' : 'list',
     use: {
       baseURL: 'http://localhost:3000',
       trace: 'on-first-retry',
     },
     projects: [
       {
         name: 'chromium',
         use: { ...devices['Desktop Chrome'] },
       },
       {
         name: 'firefox',
         use: { ...devices['Desktop Firefox'] },
       },
       // WebKit intentionally excluded — too flaky on Linux CI
     ],
     webServer: {
       command: 'npm run start',
       url: 'http://localhost:3000',
       reuseExistingServer: !process.env.CI,
       timeout: 120_000,
     },
   })
   ```

   Key decisions per CONTEXT.md:
   - Chromium + Firefox only (NOT WebKit -- too flaky on Linux CI)
   - `webServer.command` is `npm run start` (which calls `next start`) -- CI downloads the pre-built `.next/` artifact
   - `reuseExistingServer: !process.env.CI` -- CI always gets a fresh server
   - `forbidOnly: !!process.env.CI` -- prevents `.only` from sneaking into CI
   - `reporter: 'github'` in CI for GitHub Actions annotations

4. Create `e2e/` directory and `e2e/smoke.spec.ts` with this content:
   ```typescript
   import { test, expect } from '@playwright/test'

   test('unauthenticated root redirects to login page', async ({ page }) => {
     await page.goto('/')

     // proxy.ts redirects unauthenticated users to /login
     await expect(page).toHaveURL('/login')

     // Login page renders with the heading
     // Note: Login form inputs will be added in Phase 3 (Auth)
     // For now, assert the placeholder heading is visible
     await expect(page.getByRole('heading', { name: 'Login' })).toBeVisible()
   })
   ```

   IMPORTANT: The current login page has NO form inputs -- it only has an h1 "Login" and a paragraph. The smoke test asserts the heading text, NOT `input[type="email"]`. This will be updated in Phase 3 when the real login form is built.

   ALSO IMPORTANT: The smoke test depends on proxy.ts existing to redirect `/` to `/login`. If proxy.ts does not exist yet, the test may need to assert that the page loads at `/` instead. Check whether `src/proxy.ts` exists. If it does NOT exist, adjust the test to navigate directly to `/login` and assert the heading:
   ```typescript
   test('login page renders', async ({ page }) => {
     await page.goto('/login')
     await expect(page.getByRole('heading', { name: 'Login' })).toBeVisible()
   })
   ```

5. Verify Playwright runs locally (this requires a built app):
   ```bash
   npm run build && npx playwright test --project=chromium
   ```
   If proxy.ts doesn't exist and the redirect doesn't work, update the test accordingly.
  </action>
  <verify>
    <automated>npx playwright test --project=chromium 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` exists at repo root
    - `playwright.config.ts` contains `testDir: './e2e'`
    - `playwright.config.ts` contains `webServer` block with `command: 'npm run start'`
    - `playwright.config.ts` contains `devices['Desktop Chrome']` and `devices['Desktop Firefox']`
    - `playwright.config.ts` does NOT contain WebKit or Safari
    - `playwright.config.ts` contains `reuseExistingServer: !process.env.CI`
    - `playwright.config.ts` contains `forbidOnly: !!process.env.CI`
    - `e2e/smoke.spec.ts` exists
    - `e2e/smoke.spec.ts` contains `toHaveURL` or `getByRole('heading'`
    - `e2e/smoke.spec.ts` imports from `@playwright/test`
    - `package.json` devDependencies contains `@playwright/test`
    - `npx playwright test --project=chromium` passes (exit 0)
  </acceptance_criteria>
  <done>Playwright is installed with Chromium and Firefox, config file created with webServer and two browser projects, smoke test passes locally proving the app boots and the login page renders.</done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (confirm all scripts exist: lint, test, type-check, build, start)
    - .env.example (env var names for CI environment)
    - playwright.config.ts (confirm webServer config for e2e job)
    - docker-compose.yml (Postgres 16 and Redis 7 versions to match in CI)
    - .planning/phases/02-ci-cd-and-test-infrastructure/02-RESEARCH.md (full CI workflow pattern)
  </read_first>
  <action>
1. Create `.github/workflows/` directory:
   ```bash
   mkdir -p .github/workflows
   ```

2. Create `.github/workflows/ci.yml` with this exact content:
   ```yaml
   name: CI

   on:
     push:
       branches: [main]
     pull_request:
       branches: [main]

   jobs:
     test:
       runs-on: ubuntu-latest
       services:
         postgres:
           image: postgres:16-alpine
           env:
             POSTGRES_DB: potplanner
             POSTGRES_USER: potplanner
             POSTGRES_PASSWORD: potplanner
           options: >-
             --health-cmd pg_isready
             --health-interval 10s
             --health-timeout 5s
             --health-retries 5
           ports:
             - 5432:5432
         redis:
           image: redis:7-alpine
           options: >-
             --health-cmd "redis-cli ping"
             --health-interval 10s
             --health-timeout 5s
             --health-retries 5
           ports:
             - 6379:6379
       env:
         DATABASE_URL: postgresql://potplanner:potplanner@localhost:5432/potplanner
         REDIS_URL: redis://localhost:6379
         JWT_SECRET: ci-test-secret
         NODE_ENV: test
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v6
           with:
             node-version: 24
             cache: 'npm'
         - run: npm ci
         - run: npm run db:migrate
         - run: npm run lint
         - run: npm run type-check
         - run: npm test
         - run: npm run build
         - uses: actions/upload-artifact@v4
           with:
             name: next-build
             path: .next/
             retention-days: 1

     e2e:
       needs: test
       runs-on: ubuntu-latest
       services:
         postgres:
           image: postgres:16-alpine
           env:
             POSTGRES_DB: potplanner
             POSTGRES_USER: potplanner
             POSTGRES_PASSWORD: potplanner
           options: >-
             --health-cmd pg_isready
             --health-interval 10s
             --health-timeout 5s
             --health-retries 5
           ports:
             - 5432:5432
         redis:
           image: redis:7-alpine
           options: >-
             --health-cmd "redis-cli ping"
             --health-interval 10s
             --health-timeout 5s
             --health-retries 5
           ports:
             - 6379:6379
       env:
         DATABASE_URL: postgresql://potplanner:potplanner@localhost:5432/potplanner
         REDIS_URL: redis://localhost:6379
         JWT_SECRET: ci-test-secret
         NODE_ENV: production
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v6
           with:
             node-version: 24
             cache: 'npm'
         - run: npm ci
         - run: npx playwright install chromium firefox --with-deps
         - run: npm run db:migrate
         - uses: actions/download-artifact@v4
           with:
             name: next-build
             path: .next/
         - run: npx playwright test
         - uses: actions/upload-artifact@v4
           if: failure()
           with:
             name: playwright-report
             path: playwright-report/
             retention-days: 7
   ```

   Key decisions per CONTEXT.md (all locked):
   - Two jobs: `test` and `e2e` -- `e2e` has `needs: test`
   - Triggers: `push` to `main` + `pull_request` targeting `main` only
   - Node 24 (matches Docker base image)
   - `actions/setup-node@v6` with `cache: 'npm'` -- automatic hashFiles-based caching
   - Postgres 16-alpine and Redis 7-alpine as service containers with health checks and explicit port mapping
   - `db:migrate` runs before tests in both jobs (catches migration errors)
   - `test` job uploads `.next/` artifact; `e2e` job downloads it (no redundant build)
   - `e2e` job also needs Postgres + Redis service containers (app needs DB connection to start)
   - Playwright report uploaded on failure for debugging
   - `NODE_ENV: test` for test job, `NODE_ENV: production` for e2e job

3. Verify the YAML is valid:
   ```bash
   cat .github/workflows/ci.yml | head -5
   ```
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml | grep -c "needs: test" && cat .github/workflows/ci.yml | grep -c "actions/setup-node@v6" && cat .github/workflows/ci.yml | grep -c "cache: 'npm'" && cat .github/workflows/ci.yml | grep -c "postgres:16-alpine" && cat .github/workflows/ci.yml | grep -c "redis:7-alpine"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - ci.yml contains `name: CI`
    - ci.yml contains `push:` with `branches: [main]`
    - ci.yml contains `pull_request:` with `branches: [main]`
    - ci.yml contains two job definitions: `test:` and `e2e:`
    - ci.yml `e2e` job contains `needs: test`
    - ci.yml contains `actions/setup-node@v6` with `cache: 'npm'`
    - ci.yml contains `node-version: 24`
    - ci.yml contains `postgres:16-alpine` as a service in both jobs
    - ci.yml contains `redis:7-alpine` as a service in both jobs
    - ci.yml contains `--health-cmd pg_isready` for postgres health check
    - ci.yml contains `--health-cmd "redis-cli ping"` for redis health check
    - ci.yml contains explicit port mappings `5432:5432` and `6379:6379`
    - ci.yml contains `DATABASE_URL: postgresql://potplanner:potplanner@localhost:5432/potplanner`
    - ci.yml contains `REDIS_URL: redis://localhost:6379`
    - ci.yml contains `JWT_SECRET: ci-test-secret`
    - ci.yml `test` job steps include: `npm run db:migrate`, `npm run lint`, `npm run type-check`, `npm test`, `npm run build`
    - ci.yml `test` job contains `actions/upload-artifact@v4` uploading `.next/`
    - ci.yml `e2e` job contains `actions/download-artifact@v4` downloading `next-build`
    - ci.yml `e2e` job contains `npx playwright install chromium firefox --with-deps`
    - ci.yml `e2e` job contains `npx playwright test`
    - ci.yml `e2e` job contains conditional artifact upload on failure for playwright-report
  </acceptance_criteria>
  <done>Complete CI workflow created with two jobs (test + e2e), Postgres 16 and Redis 7 service containers with health checks, hashFiles-based npm caching, build artifact sharing between jobs, and Playwright report upload on failure.</done>
</task>

</tasks>

<verification>
- `playwright.config.ts` exists with webServer, Chromium, and Firefox projects
- `e2e/smoke.spec.ts` exists and passes locally with `npx playwright test --project=chromium`
- `.github/workflows/ci.yml` exists with two jobs (`test` and `e2e`)
- `e2e` job depends on `test` job (`needs: test`)
- Both jobs have Postgres 16 and Redis 7 service containers with health checks
- `actions/setup-node@v6` with `cache: 'npm'` used in both jobs
- Build artifact is shared from `test` to `e2e` via actions/upload-artifact and actions/download-artifact
</verification>

<success_criteria>
Playwright smoke test passes locally against a built app. The CI workflow file defines the complete two-job pipeline: `test` (lint, type-check, vitest, build) and `e2e` (Playwright against downloaded build artifact). Both jobs wire Postgres 16 and Redis 7 service containers for immediate use by future phases.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ci-cd-and-test-infrastructure/02-02-SUMMARY.md`
</output>

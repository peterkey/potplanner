---
phase: 03-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/redis.ts
  - src/lib/auth/session.ts
  - src/lib/auth/rate-limit.ts
  - .env.example
  - src/lib/auth/__tests__/session.test.ts
  - src/tests/mocks/redis.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-05
  - AUTH-06
must_haves:
  truths:
    - "verifySession() throws when no cookie is present"
    - "verifySession() throws when JWT signature is invalid"
    - "verifySession() throws when jti is blacklisted in Redis"
    - "signSession() creates a JWT with sub, jti, exp claims and sets httpOnly cookie with maxAge 7 days"
    - "destroySession() writes jti to Redis blacklist with TTL matching remaining JWT lifetime"
    - "loginRateLimiter is configured with 5 points per 15-minute window"
  artifacts:
    - path: "src/lib/redis.ts"
      provides: "ioredis singleton"
      contains: "new Redis"
    - path: "src/lib/auth/session.ts"
      provides: "signSession, verifySession, destroySession"
      exports: ["signSession", "verifySession", "destroySession"]
    - path: "src/lib/auth/rate-limit.ts"
      provides: "loginRateLimiter singleton"
      exports: ["loginRateLimiter"]
    - path: "src/lib/auth/__tests__/session.test.ts"
      provides: "Unit tests for session helpers"
      min_lines: 60
  key_links:
    - from: "src/lib/auth/session.ts"
      to: "src/lib/redis.ts"
      via: "import { redis }"
      pattern: "import.*redis.*from.*@/lib/redis"
    - from: "src/lib/auth/rate-limit.ts"
      to: "src/lib/redis.ts"
      via: "import { redis }"
      pattern: "import.*redis.*from.*@/lib/redis"
---

<objective>
Create the auth infrastructure layer: Redis singleton, JWT session helpers (sign, verify, destroy), and login rate limiter.

Purpose: These are the foundational building blocks that every subsequent auth plan depends on. signSession is used by the login route, verifySession is used by every DAL function, destroySession is used by the logout route, and the rate limiter protects the login endpoint.

Output: 4 source files (redis.ts, session.ts, rate-limit.ts, redis mock) + 1 test file + updated .env.example
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-authentication/03-RESEARCH.md

<interfaces>
<!-- Existing types from the codebase that this plan needs -->

From src/lib/db/schema.ts:
```typescript
export const users = pgTable('users', {
  id: serial('id').primaryKey(),
  email: varchar('email', { length: 255 }).notNull().unique(),
  passwordHash: varchar('password_hash', { length: 255 }).notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
})
```

From src/lib/db/index.ts:
```typescript
import 'server-only'
import { drizzle } from 'drizzle-orm/node-postgres'
import * as schema from './schema'
export const db = drizzle(process.env.DATABASE_URL!, { schema })
```

From .env.example:
```
DATABASE_URL=postgresql://potplanner:potplanner@localhost:5432/potplanner
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-production
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Redis singleton and auth session helpers</name>
  <files>src/lib/redis.ts, src/lib/auth/session.ts, src/lib/auth/rate-limit.ts, .env.example</files>
  <read_first>
    - src/lib/db/index.ts (pattern for server-only singleton)
    - src/lib/db/schema.ts (users table shape — id is serial/number)
    - .env.example (existing env vars)
    - .planning/phases/03-authentication/03-RESEARCH.md (exact API patterns for jose, rate-limiter-flexible)
  </read_first>
  <action>
**Install new dependencies:**
```bash
npm install bcryptjs jose rate-limiter-flexible
npm install --save-dev @types/bcryptjs
```

Note: jose may already be a transitive dependency, but install it explicitly for direct imports.

**Create `src/lib/redis.ts`:**
```typescript
import 'server-only'
import Redis from 'ioredis'

export const redis = new Redis(process.env.REDIS_URL!)
```
- First line MUST be `import 'server-only'`
- Export a singleton `redis` instance created from `process.env.REDIS_URL!`
- Use default import `Redis` from `ioredis` (not named import)

**Create `src/lib/auth/session.ts`:**
```typescript
import 'server-only'
import { SignJWT, jwtVerify } from 'jose'
import { cookies } from 'next/headers'
import { redis } from '@/lib/redis'
```

Constants:
- `const SECRET = new TextEncoder().encode(process.env.JWT_SECRET!)`
- `const COOKIE_NAME = 'session'`
- `const JWT_EXPIRY = '7d'`
- `const MAX_AGE = 60 * 60 * 24 * 7` (7 days in seconds)

Export three async functions:

1. `signSession(userId: number): Promise<void>`
   - Generate jti via `crypto.randomUUID()`
   - Create JWT: `new SignJWT({ sub: String(userId) }).setProtectedHeader({ alg: 'HS256' }).setJti(jti).setIssuedAt().setExpirationTime(JWT_EXPIRY).sign(SECRET)`
   - Set cookie: `(await cookies()).set(COOKIE_NAME, token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', path: '/', maxAge: MAX_AGE })`
   - CRITICAL: `maxAge: MAX_AGE` makes the cookie persistent across browser restarts (AUTH-02). Without it, cookie is session-only.

2. `verifySession(): Promise<{ userId: number; jti: string }>`
   - Read cookie: `(await cookies()).get(COOKIE_NAME)?.value`
   - If no token, throw `new Error('No session cookie')`
   - Verify JWT: `const { payload } = await jwtVerify(token, SECRET)`
   - If `!payload.sub || !payload.jti`, throw `new Error('Malformed token payload')`
   - Check Redis blacklist: `const blacklisted = await redis.get(\`blacklist:${payload.jti}\`)`
   - If blacklisted, throw `new Error('Token has been revoked')`
   - Return `{ userId: Number(payload.sub), jti: payload.jti as string }`

3. `destroySession(): Promise<void>`
   - Read cookie token
   - If token exists, try to verify and extract jti + exp
   - Compute TTL: `Math.max(0, payload.exp! - Math.floor(Date.now() / 1000))`
   - Write to Redis: `await redis.setex(\`blacklist:${payload.jti}\`, ttl, '1')`
   - Delete cookie: `(await cookies()).delete(COOKIE_NAME)`
   - If token is already invalid (catch block), still delete the cookie

**Create `src/lib/auth/rate-limit.ts`:**
```typescript
import 'server-only'
import { RateLimiterRedis } from 'rate-limiter-flexible'
import { redis } from '@/lib/redis'

export const loginRateLimiter = new RateLimiterRedis({
  storeClient: redis,
  keyPrefix: 'login_fail',
  points: 5,
  duration: 60 * 15,
  blockDuration: 60 * 15,
})
```

**Update `.env.example`:**
Add two new env vars for the seed script (Plan 02 will use these):
```
HOUSEHOLD_EMAIL=household@potplanner.local
HOUSEHOLD_PASSWORD=change-me-in-production
```
Append these after the existing `JWT_SECRET` line. Do NOT remove existing vars.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/redis.ts exists and contains `import 'server-only'` as first line
    - src/lib/redis.ts contains `new Redis(process.env.REDIS_URL!)`
    - src/lib/auth/session.ts exists and contains `import 'server-only'` as first line
    - src/lib/auth/session.ts contains `export async function signSession(userId: number)`
    - src/lib/auth/session.ts contains `export async function verifySession()`
    - src/lib/auth/session.ts contains `export async function destroySession()`
    - src/lib/auth/session.ts contains `maxAge: MAX_AGE` in the cookie set call
    - src/lib/auth/session.ts contains `redis.get(\`blacklist:${payload.jti}\`)`
    - src/lib/auth/session.ts contains `redis.setex(\`blacklist:`
    - src/lib/auth/rate-limit.ts exists and contains `import 'server-only'` as first line
    - src/lib/auth/rate-limit.ts contains `points: 5`
    - src/lib/auth/rate-limit.ts contains `duration: 900` or `duration: 60 * 15`
    - .env.example contains `HOUSEHOLD_EMAIL=`
    - .env.example contains `HOUSEHOLD_PASSWORD=`
    - `npm run type-check` exits 0
    - package.json contains `bcryptjs` in dependencies
    - package.json contains `jose` in dependencies
    - package.json contains `rate-limiter-flexible` in dependencies
  </acceptance_criteria>
  <done>Redis singleton, session helpers (sign/verify/destroy), and rate limiter all exist, compile, and follow server-only boundary rules</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for session helpers</name>
  <files>src/tests/mocks/redis.ts, src/lib/auth/__tests__/session.test.ts</files>
  <read_first>
    - src/lib/auth/session.ts (the module under test)
    - src/lib/redis.ts (what needs mocking)
    - vitest.config.mts (test configuration)
    - src/tests/setup.ts (existing test setup if any)
  </read_first>
  <behavior>
    - Test: verifySession throws "No session cookie" when cookies().get returns undefined
    - Test: verifySession throws "Invalid or expired session token" when JWT has wrong signature
    - Test: verifySession throws "Token has been revoked" when jti is found in Redis blacklist
    - Test: verifySession returns { userId, jti } for a valid non-blacklisted token
    - Test: signSession sets cookie with httpOnly true and maxAge 604800 (7 days)
    - Test: destroySession writes jti to Redis blacklist with correct TTL
    - Test: destroySession deletes cookie even when token is already invalid
  </behavior>
  <action>
**Create `src/tests/mocks/redis.ts`:**
A mock for the ioredis client that the session module imports. This mock should:
- Export a `mockRedis` object with `get: vi.fn()`, `setex: vi.fn()`, `del: vi.fn()` methods
- Use `vi.mock('@/lib/redis', () => ({ redis: mockRedis }))` pattern in tests

**Create `src/lib/auth/__tests__/session.test.ts`:**
- Mock `@/lib/redis` using the shared mock
- Mock `next/headers` cookies() to return a controllable mock cookie store:
  ```typescript
  const mockCookieStore = {
    get: vi.fn(),
    set: vi.fn(),
    delete: vi.fn(),
  }
  vi.mock('next/headers', () => ({
    cookies: vi.fn().mockResolvedValue(mockCookieStore),
  }))
  ```
- Mock `server-only` to be a no-op: `vi.mock('server-only', () => ({}))`
- For JWT-related tests, use the real `jose` library to create valid/invalid tokens

Test cases:
1. `verifySession — no cookie` — mockCookieStore.get returns undefined — expect error "No session cookie"
2. `verifySession — bad signature` — set cookie to a JWT signed with wrong secret — expect error containing "Invalid"
3. `verifySession — blacklisted jti` — sign a valid JWT, set mockRedis.get to return '1' for the jti — expect error "Token has been revoked"
4. `verifySession — valid token` — sign JWT with correct secret, mockRedis.get returns null — expect { userId: 1, jti: <the jti> }
5. `signSession — cookie attributes` — call signSession(1), assert mockCookieStore.set was called with args where options include `httpOnly: true` and `maxAge: 604800`
6. `destroySession — blacklist write` — set cookie to valid JWT with known jti and exp, call destroySession() — assert mockRedis.setex called with key `blacklist:<jti>`, TTL > 0, value '1'
7. `destroySession — invalid token still clears cookie` — set cookie to garbage, call destroySession() — assert mockCookieStore.delete called with 'session'
  </action>
  <verify>
    <automated>npx vitest run src/lib/auth/__tests__/session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/mocks/redis.ts exists
    - src/lib/auth/__tests__/session.test.ts exists
    - src/lib/auth/__tests__/session.test.ts contains at least 7 test cases (grep for `it(` or `test(` returns >= 7)
    - src/lib/auth/__tests__/session.test.ts contains `vi.mock('server-only'`
    - src/lib/auth/__tests__/session.test.ts contains `vi.mock('next/headers'`
    - `npx vitest run src/lib/auth/__tests__/session.test.ts` exits 0
  </acceptance_criteria>
  <done>All 7 session helper tests pass, covering no-cookie, bad-signature, blacklisted, valid-token, cookie-attributes, blacklist-write, and invalid-token-cleanup scenarios</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes (TypeScript compilation)
- `npx vitest run src/lib/auth/__tests__/session.test.ts` passes (all 7 tests green)
- All auth files contain `import 'server-only'` as first import
</verification>

<success_criteria>
- Redis singleton exports a single ioredis client
- signSession creates JWT with sub, jti, iat, exp claims and sets httpOnly cookie with 7-day maxAge
- verifySession reads cookie, validates JWT signature, checks Redis blacklist, returns userId + jti
- destroySession blacklists jti in Redis with correct TTL and deletes cookie
- loginRateLimiter configured for 5 attempts per 15 minutes
- 7 unit tests pass covering all session helper paths
</success_criteria>

<output>
After completion, create `.planning/phases/03-authentication/03-01-SUMMARY.md`
</output>
